<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" 
          android:installLocation="auto" 
          package="com.tyrrrz.youtubedownloader" 
          android:versionCode="1" 
          android:versionName="1.0.0.0">

    <!-- Basic permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    
    <!-- Storage permissions for Android 10 and below -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    
    <!-- Storage permission for Android 11+ (API 30+) -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    
    <!-- Service permissions for later probably
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" /> -->
    
    <!-- Notification permission for Android 13+ (API 33+) for later probably
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" /> -->
    
    <!-- Media permissions for Android 13+ (API 33+) for later probably
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" /> -->
    
    <!-- Request legacy external storage for Android 10 (API 29) -->
    <application android:label="YoutubeDownloader" 
                 android:icon="@drawable/Icon" 
                 android:requestLegacyExternalStorage="true" />
</manifest>